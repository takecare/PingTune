<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <TextView
        android:id="@+id/cardinalityTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/cardinality_text_margin"
        android:singleLine="true"
        android:textSize="@dimen/cardinality_title_text_size"
        tools:text="@string/app_name" />

    <TextView
        android:id="@+id/cardinalityValueTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/cardinality_text_margin"
        android:singleLine="true"
        android:textSize="@dimen/cardinality_value_text_size"
        tools:text="@string/app_name" />

</LinearLayout>